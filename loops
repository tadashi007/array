pragma solidity ^0.8.0;

contract LoopExample {
    function performOperation() external pure returns (uint) {
        uint result = 0;
        
        for (uint i = 0; i < 1000; i++) {
            result = result + i; // Add current loop index to the result
        }
        
        return result;
    }
}
